---

### 2. `docs/ai-usage-report.md`
*(Place this inside your `docs` folder. It includes the required Phase 4 updates.)*

```markdown
# 🤖 Comprehensive AI Usage Report (Assignments 1, 2, 3 & 4)

**Student:** Mohammed Alzaid (202272600)  
**Project:** Interactive Portfolio Website  
**Tools Used:** Gemini, VS Code, Git, GitHub.

---

## 1. Project Overview & Tools
This report documents the use of AI-assisted development across four phases, culminating in a production-ready portfolio. 

## 2. Phase 1: Foundation & Repository Setup (Assignment 1)
* **Repository Setup:** Guided me through cloning a remote GitHub repository to my local environment using the Command Prompt.
* **Semantic Structure:** Assisted in generating the initial HTML5 boilerplate and CSS Grid structure.

## 3. Phase 2: Interactivity & State Management (Assignment 2)
* **Persistent Theme Engine:** Guided the implementation of the **Web Storage API** using `localStorage` to save the Dark Mode preference.
* **Micro-Animations:** AI provided the CSS `transform` and `transition` properties required to create professional "lift" effects.

## 4. Phase 3: Advanced Functionality & APIs (Assignment 3)
* **API Integration (GitHub):** The AI provided the standard structure for `async/await` fetch requests and showed me how to handle HTTP errors cleanly.
* **Project Filtering Logic:** Helped me implement a data-attribute filtering system (`data-category`). 
* **Performance Optimization:** Suggested and demonstrated "debouncing" on the contact form button using `setTimeout`.

## 5. Phase 4: Final Polish & Innovation (Assignment 4)
For the final phase, I used AI to generate a creative "Innovation" feature.
* **Typewriter Effect:** I requested a JavaScript function that simulates a terminal typing effect to match my Cybersecurity focus. The AI provided a recursive function using `setTimeout` and `charAt()` to slowly reveal the text string, along with the CSS required for a blinking cursor animation.

### Phase 4 Example Prompt:
* *"How can I create a typing effect in vanilla JavaScript that makes it look like a terminal typing out my name?"*

## 6. Learning Outcomes
Through this AI-assisted process, I learned:
1. **Asynchronous JS:** Mastering `async/await` and the `fetch` API.
2. **State Management:** Using `localStorage` to handle persistent user preferences.
3. **DOM Manipulation:** Targeting specific IDs and iterating over NodeLists to update content dynamically.

## 7. Responsible Use & Modifications
To ensure academic integrity, I did not blindly copy the AI's output. When generating the typewriter effect, I manually adjusted the millisecond timing in the `setTimeout` function to ensure the typing speed felt natural, and I tied the CSS cursor color directly to my existing CSS variables (`var(--primary-blue)`).